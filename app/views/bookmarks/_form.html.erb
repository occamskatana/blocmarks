
	<%= simple_form_for [@topic, @topic.bookmarks.new], id: 'new-bookmark-form', remote: true do |f| %>
		<%= f.input :url, placeholder: "Enter URL Here", label: false %>
		<%= f.button :submit, class: 'btn btn-success btn-xs' %>
	<% end %>
